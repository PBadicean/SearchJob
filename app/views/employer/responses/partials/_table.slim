tr
  th Vacancy
  th Response
  th Candidate
- responses.each do |response|
  tr
    td = link_to "#{response.vacancy.name}", vacancy_path(response.vacancy)
    td = link_to "#{response.discription}", employer_response_path(response)
    td = response.candidate.email

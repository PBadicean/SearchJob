- response.messages.each do |message|
  - if message.persisted?
    ul.chat
      li.right.clearfix
        .header
          strong.pull-right.primary-font= message.user.name
          small.text-muted
            span.glyphicon.glyphicon-time
            = message.created_at.strftime('%r')
        span.chat-img.pull-right
          .row
            .col-md-1
              - if message.user.candidate?
                = image_tag "man_candidate.png", alt: "", size: "60x50"
              - else
                  = image_tag "man_employer.png", alt: "", size: "60x50"
            .col-md-11
              p= message.message_text
